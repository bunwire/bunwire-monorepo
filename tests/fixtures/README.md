# Compiler fixtures

Future compiler milestones place isolated TypeScript fixture projects here. Keeping the fixture root in the repository foundation avoids coupling compiler tests to package internals.
